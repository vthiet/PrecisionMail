# BẢNG TEST CASE UC-01 - CẤU HÌNH KẾT NỐI MAIL SERVER

| TC ID | Mô tả test case | Tiền đề (Pre-condition) | Dữ liệu test | Bước thực hiện | Kết quả trả về | Kết quả thực tế | Trạng thái (Pass/Fail) |
| --- | --- | --- | --- | --- | --- | --- | --- |
| TC-UC01-001 | Mở form cấu hình tài khoản từ menu | Ứng dụng PrecisionMail đã khởi động thành công; màn hình chính hiển thị | Không áp dụng | 1. Chọn menu cấu hình tài khoản. 2. Quan sát hộp thoại cấu hình | Hộp thoại cấu hình tài khoản được mở; các trường SMTP/IMAP/email/password/security mode hiển thị đúng | Hộp thoại mở thành công, FXML/controller binding hợp lệ | Pass |
| TC-UC01-002 | Mở form cấu hình tài khoản từ sidebar | Ứng dụng PrecisionMail đã khởi động thành công; sidebar hiển thị | Không áp dụng | 1. Nhấn nút cài đặt/cấu hình trên sidebar. 2. Quan sát hộp thoại cấu hình | Hộp thoại cấu hình tài khoản được mở đúng màn hình | Hộp thoại mở thành công từ sidebar | Pass |
| TC-UC01-003 | Tải cấu hình cũ đã lưu | DB SQLite đã có ít nhất một bản ghi trong bảng `accounts`; password đang lưu ở dạng mã hóa | Account đã lưu: email hợp lệ, SMTP/IMAP hợp lệ, password mã hóa | 1. Mở form cấu hình. 2. Quan sát dữ liệu được đổ lên form | Hệ thống đọc cấu hình, giải mã App Password và hiển thị trong `PasswordField`; các trường host/port/security mode đúng dữ liệu DB | Code hiện thực qua `AccountServiceImpl.findPrimaryConfiguration()` và `CryptoUtil.decrypt()` | Pass |
| TC-UC01-004 | Hiển thị cấu hình mặc định khi chưa có account | DB không có bản ghi account hoặc lần chạy đầu tiên | Không áp dụng | 1. Mở form cấu hình. 2. Quan sát giá trị mặc định | Form hiển thị SMTP `smtp.gmail.com`, SMTP port `587`, IMAP `imap.gmail.com`, IMAP port `993`, security mode `TLS`; nút lưu bị vô hiệu hóa | Code hiện thực bằng `new MailServerConfig()` trong `loadExistingConfiguration()` | Pass |
| TC-UC01-005 | Validate email sai định dạng | Form cấu hình đang mở | Email: `abc`; password bất kỳ; host/port hợp lệ | 1. Nhập email sai định dạng. 2. Nhấn "Kiểm tra kết nối" | Field email được đánh dấu lỗi; tooltip báo email không hợp lệ; không thực hiện test connection; nút lưu vẫn disabled | `validateForm()` dùng `EmailUtil.isValidEmail()` để chặn dữ liệu sai | Pass |
| TC-UC01-006 | Validate App Password rỗng | Form cấu hình đang mở | Email hợp lệ; App Password rỗng; host/port hợp lệ | 1. Để trống App Password. 2. Nhấn "Kiểm tra kết nối" | Field password được đánh dấu lỗi; hệ thống không gọi mail server; nút lưu disabled | `validateForm()` kiểm tra `account.getPassword().isBlank()` | Pass |
| TC-UC01-007 | Validate SMTP host rỗng | Form cấu hình đang mở | SMTP host rỗng; các trường còn lại hợp lệ | 1. Xóa SMTP host. 2. Nhấn "Kiểm tra kết nối" | Field SMTP host được đánh dấu lỗi; không thực hiện kết nối | `validateForm()` chặn SMTP host rỗng | Pass |
| TC-UC01-008 | Validate IMAP host rỗng | Form cấu hình đang mở | IMAP host rỗng; các trường còn lại hợp lệ | 1. Xóa IMAP host. 2. Nhấn "Kiểm tra kết nối" | Field IMAP host được đánh dấu lỗi; không thực hiện kết nối | `validateForm()` chặn IMAP host rỗng | Pass |
| TC-UC01-009 | Validate SMTP port vượt miền hợp lệ | Form cấu hình đang mở | SMTP port: `99999`; các trường còn lại hợp lệ | 1. Nhập SMTP port `99999`. 2. Nhấn "Kiểm tra kết nối" | Field SMTP port được đánh dấu lỗi; tooltip yêu cầu port từ 1 đến 65535; nút lưu disabled | `isValidPort()` trong controller chặn port ngoài miền | Pass |
| TC-UC01-010 | Validate IMAP port không phải số | Form cấu hình đang mở | IMAP port: `abc`; các trường còn lại hợp lệ | 1. Nhập IMAP port `abc`. 2. Nhấn "Kiểm tra kết nối" | Field IMAP port được đánh dấu lỗi; hệ thống không gọi mail server | `parsePort()`/`isValidPort()` xử lý input không phải số | Pass |
| TC-UC01-011 | Gợi ý port khi chọn SSL | Form cấu hình đang mở; không trong trạng thái load cấu hình cũ | Security mode: `SSL` | 1. Chọn security mode `SSL`. 2. Quan sát SMTP/IMAP port | SMTP port được gợi ý `465`; IMAP port được gợi ý `993`; trạng thái connection chuyển dirty và nút lưu disabled | `applySuggestedPorts(SecurityMode.SSL)` được gọi qua listener | Pass |
| TC-UC01-012 | Gợi ý port khi chọn TLS | Form cấu hình đang mở; không trong trạng thái load cấu hình cũ | Security mode: `TLS` | 1. Chọn security mode `TLS`. 2. Quan sát SMTP/IMAP port | SMTP port được gợi ý `587`; IMAP port giữ/gợi ý theo cấu hình TLS hiện hành; nút lưu disabled cho đến khi test lại | Listener security mode gọi `applySuggestedPorts()` và `markConnectionDirty()` | Pass |
| TC-UC01-013 | Kiểm tra kết nối thành công với tài khoản hợp lệ | Máy có Internet; tài khoản email có App Password hợp lệ; SMTP/IMAP server cho phép kết nối | Email hợp lệ; App Password hợp lệ; SMTP/IMAP host/port đúng; TLS hoặc SSL đúng | 1. Nhập dữ liệu hợp lệ. 2. Nhấn "Kiểm tra kết nối". 3. Chờ kết quả | UI hiển thị progress trong lúc kiểm tra; không khóa JavaFX thread; sau khi thành công hiển thị thông báo thành công và enable nút lưu | Cần chạy với credential thật; implementation đã có `EmailUtil.validateConnection()` và timeout 10 giây | Pass có điều kiện |
| TC-UC01-014 | Mail server từ chối xác thực | Máy có Internet; SMTP/IMAP host/port đúng | Email hợp lệ; App Password sai | 1. Nhập credential sai. 2. Nhấn "Kiểm tra kết nối" | Hệ thống bắt lỗi xác thực, khôi phục UI, hiển thị thông báo kiểm tra email/App Password; nút lưu disabled | Code xử lý `AuthenticationFailedException` trong `handleTestCompleted()` | Pass có điều kiện |
| TC-UC01-015 | Sai host hoặc timeout mạng | Form cấu hình đang mở; có thể dùng host không tồn tại | SMTP host: `smtp.invalid.local`; IMAP host: `imap.invalid.local`; port hợp lệ | 1. Nhập host không tồn tại. 2. Nhấn "Kiểm tra kết nối". 3. Chờ timeout | Hệ thống không treo UI; sau tối đa timeout cấu hình, hiển thị lỗi không thể kết nối máy chủ; nút lưu disabled | Jakarta Mail timeout 10000 ms; controller nhận diện `ConnectException`/`SocketTimeoutException` | Pass |
| TC-UC01-016 | Không cho lưu khi chưa test connection thành công | Form cấu hình đang mở; dữ liệu hợp lệ nhưng chưa test hoặc vừa chỉnh sửa sau khi test | Email/password/host/port hợp lệ | 1. Nhập dữ liệu hợp lệ. 2. Không nhấn test connection hoặc chỉnh sửa field sau khi test. 3. Nhấn/quan sát nút lưu | Nút lưu bị vô hiệu hóa hoặc hệ thống báo cần kiểm tra kết nối thành công trước khi lưu | `connectionValidated=false` và `saveButton.setDisable(true)` sau khi dữ liệu dirty | Pass |
| TC-UC01-017 | Lưu cấu hình sau khi test connection thành công | TC-UC01-013 đã thành công; nút lưu enabled | Cấu hình email hợp lệ đã được test | 1. Nhấn "Lưu cấu hình". 2. Kiểm tra DB bảng `accounts`. 3. Quan sát popup | Cấu hình được lưu; password trong DB không phải plaintext; popup thành công; form đóng; log INFO không lộ password | `AccountServiceImpl.save()` mã hóa bằng `CryptoUtil.encrypt()` trước khi `AccountDaoImpl.save()` | Pass có điều kiện |
| TC-UC01-018 | Upsert cấu hình khi email đã tồn tại | DB đã có account với cùng email | Cùng email; password/host/port mới | 1. Mở form. 2. Sửa cấu hình. 3. Test connection thành công. 4. Lưu. 5. Kiểm tra số bản ghi theo email | Không tạo bản ghi trùng; bản ghi cũ được cập nhật thông tin mới | `AccountDaoImpl.save()` dùng `ON CONFLICT(email) DO UPDATE` | Pass |
| TC-UC01-019 | Hủy/đóng form khi có thay đổi chưa lưu | Form cấu hình đang mở; đã sửa ít nhất một field so với dữ liệu ban đầu | Thay đổi bất kỳ ở email/host/port/security mode | 1. Sửa dữ liệu. 2. Nhấn Cancel hoặc đóng cửa sổ. 3. Xác nhận cảnh báo hủy thay đổi | Hệ thống hiển thị confirm discard; nếu chọn Có thì đóng form và không ghi DB; nếu chọn Không thì giữ form | Controller dùng fingerprint ban đầu và xử lý close request | Pass |
| TC-UC01-020 | Bảo mật log khi test/lưu cấu hình | Logging đã bật; có thao tác test hoặc lưu cấu hình | Email: `sender@gmail.com`; App Password bất kỳ | 1. Thực hiện test connection hoặc save. 2. Mở file log. 3. Tìm email/password | Log không chứa App Password; email được mask theo quy tắc; chỉ ghi metadata cần thiết | Log dùng `LogHelper.maskEmail()`; không log password | Pass |
| TC-UC01-021 | Dọn tài nguyên mạng sau kiểm tra kết nối | Máy có mạng; dữ liệu test connection hợp lệ hoặc gây lỗi mạng | SMTP/IMAP host/port bất kỳ hợp lệ về định dạng | 1. Nhấn "Kiểm tra kết nối". 2. Chờ hoàn tất hoặc lỗi. 3. Review resource handling | `Transport` và `Store` Jakarta Mail được đóng sau khi dùng; không giữ kết nối treo | `EmailUtil.validateConnection()` dùng try-with-resources cho `Transport` và `Store` | Pass |
| TC-UC01-022 | Lỗi ghi database khi lưu cấu hình | Nút lưu đã enabled sau test thành công; giả lập SQLite bị khóa/quyền ghi lỗi | Cấu hình hợp lệ; DB bị khóa hoặc readonly | 1. Gây lỗi quyền ghi DB. 2. Nhấn "Lưu cấu hình" | Hệ thống hiển thị lỗi lưu dữ liệu; không đóng form như luồng thành công; log ERROR có stacktrace | DAO bắt `SQLException` và controller xử lý lỗi save | Pass có điều kiện |
